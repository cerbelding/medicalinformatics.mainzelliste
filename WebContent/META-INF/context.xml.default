<?xml version="1.0" encoding="UTF-8"?>
<!-- 
	For the sake of readability, most of mainzelliste's parameters 
	have been moved	to a key-value conf file. Per default,
	this file resides in /etc/mainzelliste/mainzelliste.conf. If you would
	like to place the file somewhere else (e.g. to host 
	multiple mainzelliste instances on one machine), specify the
	new path below.
	
	Note that Tomcat will delete this file on undeployment.
	
	@author Martin Lablans
 -->
<Context>
        <Parameter name="de.pseudonymisierung.mainzelliste.ConfigurationFile"
                value="/etc/mainzelliste.conf" override="false" />
</Context>